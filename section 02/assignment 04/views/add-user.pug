extends layout/main-layout.pug

block styles 
    link(rel="stylesheet", href="/css/main.css")
block content
    form.form(action="/admin/add-user", method="POST")
        div.form-control
            label(for="title") Title
                input(type="text", name="title")#title

        button(type="submit") Add New User